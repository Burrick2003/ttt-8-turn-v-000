#!/usr/bin/env ruby
require_relative '../lib/turn.rb'

#def turn(board, index, user_char="X")
  puts "Please enter 1-9:"
  user_input = gets.strip
  input_to_index(user_input)
  move(board, index, user_char)
  display_board(board)
